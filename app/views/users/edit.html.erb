<% if current_user && current_user.admin %>
<h1 class="display-4">Editing User</h1>

<%= render 'form' %>

<br/>
<%= link_to 'Back', users_path, class: "btn btn-secondary" %>
<%= link_to 'Show', @user, class: "btn btn-info" %>


<% else %>
<br>
<br>
<p class="lead"><%= user_is_not_admin %></p>
<% end %>